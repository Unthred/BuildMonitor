# Build outputs
bin/
obj/
out/
publish/

# User-specific / local IDE
.vs/
.vscode/
*.user
*.suo
*.userosscache
*.sln.docstates

# NuGet
*.nupkg
**/packages/
project.lock.json
project.fragment.lock.json
artifacts/

# Test / coverage
TestResults/
coverage/
*.coverage
*.coveragexml

# OS
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# Logs and local app data (user machine)
*.log
logs/

# Secrets — never commit
.env
.env.*
*.pfx
credentials.json
secrets.json

# Control-plane agent skill install copies (canonical source: docs/ops/agent-skills/buildmonitor-control-plane/).
# Installer overwrites these under a watched repo's .cursor/; do not track them in BuildMonitor itself.
# Keep other tracked .cursor/rules and .cursor/skills (feature-kickoff, etc.).
.cursor/skills/buildmonitor-control-plane/
.cursor/rules/buildmonitor-control-plane.mdc
